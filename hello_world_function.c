#include <stdio.h>

// Define a function named 'greet' that prints greetings
void greet() { 
    printf("Hello, World!\n");
    printf("Hello, COMP106!\n");
}

int main() { // The entry point of the program
    greet(); // Call the greet() function to run its code
    return 0;
}